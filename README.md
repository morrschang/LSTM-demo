
# 🧠 LSTM 模型互動展示網站

這是由 Kaggle Notebook 轉換而成的 Streamlit 互動展示網站。

## 📦 使用方式
1. 將你的 `lstm_model.h5` 放入 `model/` 資料夾。
2. 安裝所需套件：
   ```bash
   pip install -r requirements.txt
   ```
3. 啟動網站：
   ```bash
   streamlit run app.py
   ```
4. 開啟瀏覽器後可輸入數值或上傳資料測試模型預測。

---
